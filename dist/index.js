"use strict";
let age = 20;
const isYoung = (age) => {
    if (age < 60)
        return true;
    else
        return false;
};
const resultAge = isYoung(age);
console.log(resultAge);
//# sourceMappingURL=index.js.map